global:
  ingress:
    enabled: true
    hosts:
      - example.com
      - "*.example.com"
    disableHostInIngress: false

  gatewayAPI:
    enabled: true
    parentRef:
      name: test-gateway
      namespace: gateway-system

ingress:
  objects:
    - name: api-routes
      paths:
        - path: /api/.*
          backend:
            service:
              name: test-service
              port: 8080

service:
  port: 8080
